package com.passtick.test.buisiness

import java.security.SecureRandom

class PasswordGenerator {

    fun generatePassowrd(Length : Int = 12, includedChars : List<Char>){

        var defaultCharList : MutableList<Char>

        for(char in 'a' .. 'z')
            defaultCharList.add(char)


    }
}